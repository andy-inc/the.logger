default:
  all:
    all:
      console: true
    error:
      file:
        fileName: error.log
produnction:
  $extends: default
user:
  $extends: default
  all:
    $inner: false
    error:
      console: true